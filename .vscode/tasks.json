{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build WASM (Rust)",
      "type": "shell",
      "command": "wasm-pack build --target web --release",
      "options": {
        "cwd": "${workspaceFolder}/wasm-physics"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
